#pragma once

#include <string>

namespace Blame::Styles {
    struct Symbols {
        std::string top_left;
        std::string top_middle;
        std::string top_right;
        std::string middle_left;
        std::string middle_fill;
        std::string middle_right;
        std::string bottom_left;
        std::string bottom_middle;
        std::string bottom_right;
    };
}