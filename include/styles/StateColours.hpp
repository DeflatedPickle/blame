#pragma once

#include <string>

namespace Blame::Styles {
    struct StateColours {
        std::string normal;
        std::string focused;
        std::string active;
        std::string disabled;
    };
}
